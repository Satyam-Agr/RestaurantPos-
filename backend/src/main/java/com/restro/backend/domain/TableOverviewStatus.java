package com.restro.backend.domain;

public enum TableOverviewStatus {
    AVAILABLE,
    AWAITING_ORDER,
    NEEDS_CONFIRMATION,
    READY_TO_SERVE,
    BILL_REQUESTED,
    PREPARING,
    SERVED_AWAITING_BILL
}
